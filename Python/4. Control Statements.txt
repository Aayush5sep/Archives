# Control Statements

	while condition:
		execute-statement

	for var in list:
		execute-statement

	match var:
		case value1:
			execute-stat1
		case _:
			execute-else-stat

	if condition:
		exec-stat
	elif condition:
		exec-stat
	else:
		exec-stat

	break, continue, pass