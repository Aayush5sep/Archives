# math.py
	math.pi, .pow(base,power), .factorial(integer), .floor(smaller_value), .ceil(higher_value)......

# random
	random.randint(start,end), .random()

# os
	os.getcwd() get_current_working_directory, .mkdir() make_directory, .remove('file_name'), .removedirs("dir"),
	  .open(), .rename(), .path()

# sys
	sys.version, .path

# datetime
	datetime.datetime.now()